<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 px-4 py-2 rounded-md mb-2" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea name="content" id="" placeholder="What's in your mind ?"
                class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
            <input type="submit" class="px-4 py-2 bg-yellow-500 text-black rounded-md block mt-2" value="Update post">
        </form>
    </div>
</body>

</html>